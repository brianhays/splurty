<!DOCTYPE html>
<html>
<head>
  <title>Splurty</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
	<header>
	  <div class="col-xs-3">
      <%= link_to root_path, class: 'logo' do %>
        <div class="circle <%= ['blue', 'green', 'red'].shuffle.first %>">
          <br />
          - KNOW -
          <br />
          YOU
          <br />
          CAN!
        </div>
      <% end %>
    </div>
	  <div class="col-sm-4 hidden-xs slogan">
	    Splurting out inspiring awesomeness onto the internet.
	  </div>
	  <div class="col-xs-8 col-xs-offset-1 col-sm-offset-0 col-sm-5 links">
	    <%= link_to 'Contribute', '#', :data => {:toggle => 'modal', :target => '#myModal'} %>
	    &nbsp;&nbsp;
	    <%= link_to 'About', about_path %>
	  </div>
	</header>

  <!-- Modal -->
  <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          <h4 class="modal-title" id="myModalLabel">Contribute a Quote:</h4>
        </div>
        <%= simple_form_for Quote.new do |f| %>
          <div class="modal-body">
            <div class="form-group">
              <%= f.input :saying, label: "What is the quote you want to add?", placeholder: "You get only back what you give." %>
            </div>
            <div class="form-group">
              <%= f.input :author, label: "Who is the author of the quote?", placeholder: "Greg Glassman" %>
            </div>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            <%= f.submit 'Add Quote', :class => 'btn btn-danger' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>


	<hr />
	<br class="clear" />

	<div class="container">
			<% if flash[:error].present? %>
	        <div class="col-xs-10 col-xs-offset-1 alert alert-danger">
	        <%= flash[:error].html_safe %>
	        </div>
	    	<% end %>
		<%= yield %>
	</div>

	<div class="footer">
	  <div class="container">
	    <div class="col-xs-12 col-sm-6">
        <%= link_to root_path, class: "next-quote" do %>
        More motivation please! <span class="glyphicon glyphicon-chevron-right"></span>
        <% end %>
        </div>
        <div class="col-xs-12 col-sm-6 footer-blurb">
        Proudly hacked in Atlanta, GA by Brian Hays</a><br />
        Find your <%= link_to 'daily dose of inspiration here', root_path %>.
        </div>
	  </div>
	</div>

</body>
</html>
